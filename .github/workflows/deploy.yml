name: Deploy to Dev Server via FTP

on:
  push:
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # Шаг 1: Клонирование репозитория
    - name: Checkout repository
      uses: actions/checkout@v3

    # Шаг 2: Загрузка файлов через FTP (без секретов)
    - name: Upload files to FTP server
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: clio.cityhost.com.ua
        username: chf6b5697a_jfit
        password: npBlxJoruf
        port: 21
        protocol: ftp
        local-dir: ./wp-content/
        server-dir: /var/www/chf6b5697a/www/jfit.beautiful-website.space/wp-content/
        exclude: |
          .git*
          node_modules/
          *.md
          *.zip